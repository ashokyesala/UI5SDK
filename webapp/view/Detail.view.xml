<mvc:View xmlns:core="sap.ui.core" xmlns:mvc="sap.ui.core.mvc" xmlns="sap.m" controllerName="sap.ui5sdk.walkthrough.controller.Detail"
	xmlns:html="http://www.w3.org/1999/xhtml">
		<Page 
			title="{i18n>detailPageTitle}" 
			showNavButton="true" 
			navButtonPress=".onNavPress" >
			<ObjectHeader 
				responsive="true"
				number="{
					parts:[{path:'invoice>ExtendedPrice'}, {path:'view>currency'}],
					type:'sap.ui.model.type.Currency',
					formatOptions:{
						showMeasure:false
					}
				}"
				numberUnit="{view>currency}"
				intro="{invoice>ShipperName}" 
				title="{invoice>ProductName}">
				<attributes>
					<ObjectAttribute
						title="{i18n>quantityTitle}" text="{invoice>Quantity}"></ObjectAttribute>
					<ObjectAttribute
						title="{i18n>dateTitle}"
						text="{path: 'invoice>ShippedDate',
							   type: 'sap.ui.model.type.Date',
							   formatOptions:{
									style:'long',
									source:{
										pattern:'yyyy-MM-ddTHH:mm:ss'
									}
							   }}">
						
					</ObjectAttribute>
				</attributes>
			</ObjectHeader>
		</Page>
</mvc:View>
